{
  "name": "idtapthat",
  "private": true,
  "version": "1.0.0",
  "description": "I'd Tap That – MTG Life Calculator & Tournament Manager (static PWA, no build step)",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
